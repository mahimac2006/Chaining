{"thought": "I have the vulnerable function body and type definition. Now I will write a minimal pass-through harness and a symbolic-first driver that sets up ndo->ndo_snapend and a valid TCP payload so smb_tcp_print reaches the memcmp sink.", "tool": "WriteHarness", "args": {"code": "#include <stdio.h>\n#include <stdint.h>\n#include <stdlib.h>\n#include <string.h>\n#include <klee/klee.h>\n#include \"tcpdump-stdinc.h\"\n#include \"interface.h\"\n\n#ifndef ND_PRINT\n#define ND_PRINT(x) do { } while (0)\n#endif\n\n#ifndef EXTRACT_24BITS\n#define EXTRACT_24BITS(p) ((((uint32_t)((const uint8_t *)(p))[0]) << 16) | (((uint32_t)((const uint8_t *)(p))[1]) << 8) | ((uint32_t)((const uint8_t *)(p))[2]))\n#endif\n\nstatic const char *tstr = \"[|smb]\";\nstatic const u_char *startbuf;\n\nvoid print_smb(netdissect_options *ndo, const u_char *data, const u_char *maxbuf) {\n    (void)ndo; (void)data; (void)maxbuf;\n}\n\nvoid smb_tcp_print(netdissect_options *ndo,\n              const u_char * data, int length)\n{\n    int caplen;\n    u_int smb_len;\n    const u_char *maxbuf;\n\n    if (length < 4)\n\tgoto trunc;\n    if (ndo->ndo_snapend < data)\n\tgoto trunc;\n    caplen = ndo->ndo_snapend - data;\n    if (caplen < 4)\n\tgoto trunc;\n    maxbuf = data + caplen;\n    smb_len = EXTRACT_24BITS(data + 1);\n    length -= 4;\n    caplen -= 4;\n\n    startbuf = data;\n    data += 4;\n\n    if (smb_len >= 4 && caplen >= 4 && memcmp(data,\"\\377SMB\",4) == 0) {\n\tif ((int)smb_len > caplen) {\n\t    if ((int)smb_len > length)\n\t\tND_PRINT((ndo, \" WARNING: Packet is continued in later TCP segments\\n\"));\n\t    else\n\t\tND_PRINT((ndo, \" WARNING: Short packet. Try increasing the snap length by %d\\n\",\n\t\t    smb_len - caplen));\n\t} else\n\t    ND_PRINT((ndo, \" \"));\n\tprint_smb(ndo, data, maxbuf > data + smb_len ? data + smb_len : maxbuf);\n    } else\n\tND_PRINT((ndo, \" SMB-over-TCP packet:(raw data or continuation?)\\n\"));\n    return;\ntrunc:\n    ND_PRINT((ndo, \"%s\", tstr));\n}\n"}, "tool": "WriteDriver", "args": {"code": "#include <stdlib.h>\n#include <stdint.h>\n#include <klee/klee.h>\n#include \"interface.h\"\n\nextern void smb_tcp_print(netdissect_options *ndo, const u_char *data, int length);\n\nint main(void) {\n    netdissect_options *ndo = (netdissect_options *)calloc(1, sizeof(netdissect_options));\n    unsigned char *buf = (unsigned char *)calloc(1, 32);\n    int len = 12;\n\n    buf[0] = 0x00;\n    buf[1] = 0x00;\n    buf[2] = 0x00;\n    buf[3] = 0x00;\n    buf[4] = 0xff;\n    buf[5] = 'S';\n    buf[6] = 'M';\n    buf[7] = 'B';\n\n    klee_make_symbolic(buf + 8, 4, \"payload_tail\");\n\n    ndo->ndo_snapend = buf + 12;\n\n    smb_tcp_print(ndo, (const u_char *)buf, len);\n    return 0;\n}\n"}}